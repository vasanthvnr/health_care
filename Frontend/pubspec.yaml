name: health_care
version: 1.0.0+1
description: A Flutter app with login, registration, and health analysis.

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.10.5+2
  image_picker: ^1.0.4
  http: ^1.2.0
  permission_handler: ^11.0.1
  cupertino_icons: ^1.0.6
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/LOGO.jpeg
